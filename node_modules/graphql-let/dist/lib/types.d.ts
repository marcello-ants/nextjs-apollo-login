export declare type CommandOpts = {
    cwd: string;
    configPath: string;
};
export declare type ConfigTypes = {
    generateDir: string;
    config?: Record<string, boolean | string>;
    schema: string;
    documents: string | string[];
    plugins: string[];
};
