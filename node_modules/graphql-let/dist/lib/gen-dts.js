"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const typescript_1 = require("typescript");
const options = {
    declaration: true,
    emitDeclarationOnly: true,
    skipLibCheck: false,
};
function genDts(inputFileName) {
    let outputText = '';
    const compilerHost = typescript_1.createCompilerHost({});
    compilerHost.writeFile = (name, text) => {
        outputText = text;
    };
    const program = typescript_1.createProgram([inputFileName], options, compilerHost);
    program.emit();
    if (!outputText) {
        throw new Error('Fails to generate .d.ts.');
    }
    return outputText;
}
exports.default = genDts;
